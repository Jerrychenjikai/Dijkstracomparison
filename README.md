This is the code of my EE that compares the performance of the Dijkstra's algorithm using different heaps.

All heaps are self-implemented

Simply replace the imported file in dijkstra.cpp to run the algorithm with different heaps
